### 코드 작성 - Gradle

# 코드 작성

## 프로젝트 구조
- 주 파일인 `/app/src/main/java/project/App.java`의 이름은 수정하지 마세요.
- 모든 문제는 반드시 이 파일(`/app/src/main/java/project/App.java`)이 시작점이 되어 실행됩니다.

## 문제 풀이 과정

아래는 문제 풀이 진행 과정에 대한 안내입니다.

✅ 주의 사항
- `App.java` 파일에 작성된 코드는 문제 해결을 위한 예시입니다. 요구사항 해결을 위해 필요하다면 자유롭게 수정할 수 있습니다.
- 코드 작성 후에는 반드시 우측 상단의 `코드 채점` 버튼을 클릭하여 채점 결과를 확인하세요.

---

1. `App.java` 파일의 내용을 확인합니다.
   - **미리 작성된 코드를 활용하거나 자유롭게 코드를 추가하여 구현할 수 있습니다.**
     - 단, 테스트 형식에 맞지 않는 구현은 오답 처리될 수 있으니 지문을 잘 읽고 정상적으로 채점될 수 있도록 코드를 작성하세요.
   - **문제 해결에 필요한 라이브러리는 자유롭게 추가할 수 있습니다.**

2. 터미널에서 아래 명령어를 입력하여 코드를 실행할 수 있습니다.
   - **빌드**
     ```bash
     $ cd Problem1 # 풀고자 하는 문제 폴더로 이동
     $ ./gradlew clean build
     ```

   - **코드 실행**
     ```bash
     $ cd Problem1 # 풀고자 하는 문제 폴더로 이동
     $ java -jar app/build/libs/app-0.0.1-SNAPSHOT.jar
     ```
   
   - **심플 브라우저**
    ```
    코드가 정상적으로 실행되면 5000 포트에서 서버가 동작합니다. API가 제공되는 Simple Browser 화면을 통해서도 데이터 확인이 가능합니다.
    ```

   - **✅ 유의사항**
     - **실행에 문제가 발생한 경우, 왼쪽 상단의 `VS Code 재시작` 버튼을 클릭하여 다시 시도하세요.**

3. 채점을 위해 우측 상단의 `코드채점` 버튼을 클릭하여 결과를 확인합니다.
   - `코드 채점` 버튼을 누르면 자동으로 '빌드' → '코드 실행' 명령어가 순서대로 실행됩니다.
   - **✅ 유의사항**
     - **패키지나 라이브러리를 추가할 경우, 반드시 `build.gradle` 파일에 기록해야 채점 환경에서 정상 실행됩니다.**

---

**이제 문제 지문 탭에서 해당 문제 번호의 지문을 읽고 요구사항을 해결하세요.**